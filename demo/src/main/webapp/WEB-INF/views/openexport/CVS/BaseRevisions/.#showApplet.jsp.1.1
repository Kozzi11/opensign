<%@page contentType="text/html;charset=UTF-8" %>
<%@page pageEncoding="UTF-8" %>
<%@ page session="false" %>
<%@ taglib uri="http://java.sun.com/jsp/jstl/core" prefix="c" %>
<%@ taglib uri="http://java.sun.com/jsp/jstl/fmt" prefix="fmt" %>
<%@ taglib prefix="form" uri="http://www.springframework.org/tags/form" %>
<html>
<head>
    <META http-equiv="Content-Type" content="text/html;charset=UTF-8">
    <title><fmt:message key="openexport.applet.title"/></title>
</head>
<body bgcolor="white">
<applet name="signing_applet" code="org.openoces.opensign.client.applet.bootstrap.BootApplet" width="${openexport.appletWidth}" height="${openexport.appletHeight}" codebase="${docBase}" archive="${docBase}/resources/opensign/${openexport.appletVersion}/OpenSign-bootstrapped.jar?time=${timestamp}" mayscript>
    <param name="ZIP_FILE_ALIAS" value="OpenExport">
    <param name="ZIP_BASE_URL" value="${docBase}/resources/opensign/${openexport.appletVersion}/plugins">
    <param name="MS_SUPPORT" value="bcjce">
    <param name="SUN_SUPPORT" value="jsse">
    <param name="STRIP_ZIP" value="yes">
    <c:if test="${not empty plugins}">
        <param name="EXTRA_ZIP_FILE_NAMES" value="${plugins}">
    </c:if>
    <param name="locale" value="${openexport.appletLocale}">
    <param name="key.store.directory" value="null">
    <param name="LOG_LEVEL" value="${openexport.loglevel}">
    <param name="sortcertificates" value="${openexport.sortcertificates}">
    <c:if test="${not empty openexport.logonto}">
        <param name="logonto" value="${openexport.logonto}">
    </c:if>
    <c:if test="${not empty openexport.issuerdnfilter}">
        <param name="issuerdnfilter" value="${openlogon.base64Issuerdnfilter}">
    </c:if>
    <c:if test="${not empty openexport.subjectdnfilter}">
        <param name="issuerdnfilter" value="${openlogon.base64subjectdnfilter}">
    </c:if>
    <c:if test="${not empty openexport.sernofilter}">
        <param name="issuerdnfilter" value="${openlogon.base64sernofilter}">
    </c:if>
    <c:if test="${not empty openexport.logonheader}">
        <param name="LOGON_HEADER" value="${openlogon.logonheader}">
    </c:if>
</applet>


</body>

</html>